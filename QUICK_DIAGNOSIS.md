# ⚡ БЫСТРАЯ ДИАГНОСТИКА

## ✅ Если РАБОТАЕТ

### То Вы Должны Видеть:

#### На Браузере (http://192.168.4.1)
```
[Camera Stream]      [← Sidebar →]
[SCAN Button]        Results: -
                     Labels: 10 UAH, etc
                     Log: 🟢 Page loaded
```

Нажимаете SCAN → увидите:
```
Results: 100_UAH (87%)
Log: 📊 GOT RESULT
```

#### На Серийном Портале (115200, COM4)
```
🔌 Web SCAN button pressed!
📸 Capturing frame...
✓ Inference completed: 245ms
📦 Detected 1 object(s)
✅ RESULT: 100_UAH (87%)
```

---

## ❌ Если НЕ РАБОТАЕТ

### Проблема #1: Результат "Processing..." и ничего не меняется

**Причина**: Браузер не получает результат от ESP32

**Check List**:
- [ ] Открыли браузер F12 (Developer Tools)
- [ ] В Console НЕ видимо ошибок?
- [ ] Видимо логи типа "[FETCH] Calling GET /scan"?
- [ ] Серийный порт показывает "🔌 Web SCAN button pressed!"?

**Решение**:
1. Перезагрузите ESP32-CAM (выкл./включ. питание)
2. Дайте системе 5 секунд стартовать
3. Откройте http://192.168.4.1 (очистите кеш Ctrl+Shift+Del перед этим)
4. Попробуйте SCAN еще раз

---

### Проблема #2: Браузер не загружается (404 или нет соединения)

**Причина**: Нет соединения с ESP32

**Check List**:
- [ ] WiFi SSID "UAH_Scanner" видима в доступных сетях?
- [ ] Пароль: "12345678" вводите правильно?
- [ ] Пинг работает? В PowerShell: `ping 192.168.4.1`
- [ ] Другой браузер? Попробуйте Chrome вместо Firefox

**Решение**:
1. Отключитесь от WiFi UAH_Scanner
2. Забудьте сеть (Remove)
3. Переподключитесь
4. Попробуйте http://192.168.4.1 еще раз

---

### Проблема #3: Серийный порт молчит (ничего не выводит)

**Причина**: Обычно неправильный COM порт или скорость

**Check List**:
- [ ] Используете COM4 (правильный)?
- [ ] Скорость 115200 baud (не 9600!)?
- [ ] Переподключили USB кабель?
- [ ] Не используется другая программа (PlatformIO)?

**Решение**:
1. Закройте все терминалы (PlatformIO Monitor, etc)
2. Переподключите USB кабель
3. Откройте новый Serial Monitor на COM4, 115200
4. Перезагрузите ESP32-CAM нажатием кнопки RESET

---

### Проблема #4: Браузер показывает "TIMEOUT - Check serial monitor"

**Причина**: Система не ответила за 5 секунд

**Check List**:
- [ ] Серийный порт показывает "🧠 SCAN #1"?
- [ ] Видимо "Capturing frame..."?
- [ ] Есть ошибка "❌ Camera Error"?

**Решение**:
- Если "Camera Error" → Камера не подключена или не инициализирована
- Если ничего нет → Система не обработала запрос

**Действие**:
1. Проверьте USB кабель камеры (распайка)
2. Посмотрите на стартовые логи в серийном портале
3. Найдите сообщение "✓ Camera initialized successfully"

---

### Проблема #5: Браузер Console показывает HTTP 404 или 500

**Причина**: Endpoint не существует на сервере

**Check List**:
- [ ] Какой точно URL вызывает ошибку? (/scan? /status? /)
- [ ] HTTP статус: 404 (не найден) или 500 (ошибка сервера)?

**Решение**:
- 404 → Перезагрузите ESP32 (может быть старая прошивка)
- 500 → Посмотрите серийный порт на ошибки

---

### Проблема #6: "Camera Error" на серийном портале

**Причина**: Камера не инициализирована

**Check List**:
- [ ] Стартовый лог показывает "Camera initialized successfully"?
- [ ] Или показывает "❌ Camera initialization FAILED"?

**Решение**:
1. Проверьте все кабели OV2640:
   - MISO (DO)
   - MOSI (DI)
   - SCK (SCK)
   - CS
   - GND, VCC
2. Перезагрузите систему
3. Если все равно ошибка → Камера неисправна

---

## 🔍 ДИАГНОСТИЧЕСКИЕ КОМАНДЫ

### В Браузере Console (F12):

```javascript
// Проверьте что /status endpoint работает
fetch('/status').then(r => r.json()).then(d => console.log(d));

// Ожидаемый результат:
// {result: "-", fps: 30, status: "OK", time: 1234567}
```

### На PowerShell терминала:

```powershell
# Проверьте связь с ESP32
ping 192.168.4.1
# Должны получить ответ за <50ms

# Проверьте /status endpoint
curl http://192.168.4.1/status
# Должны получить JSON ответ

# Проверьте /scan endpoint
curl http://192.168.4.1/scan
# Должно вернуть {"status":"OK"}
```

---

## 📊 ОЖИДАЕМЫЕ ЗНАЧЕНИЯ

| Параметр | Значение | Допустимо | Проблема |
|----------|----------|-----------|----------|
| Время Frame | 10-50ms | <100ms | Система перегружена |
| Время JPEG→RGB | 80-150ms | <200ms | Медленный процессор |
| Время Inference | 100-300ms | <500ms | Долгое вычисление |
| Итого Сканирование | 200-500ms | <1000ms | Задержка |
| PSRAM | 230 KB allocate | < 2MB | Достаточно памяти |

---

## 🆘 ЕСЛИ НИЧЕГО НЕ ПОМОГАЕТ

1. **Полный рестарт**:
   - Отключить питание ESP32 (5 сек)
   - Включить питание
   - Дать 10 секунд стартовать
   - Открыть Serial Monitor (Ctrl+Shift+P → "Monitor")
   - Смотреть логи

2. **Очистить все**:
   - Закрыть браузер полностью
   - Забыть WiFi сеть
   - Отключить ESP32
   - Тамга 30 секунд  
   - Включить и подключиться заново

3. **Перезагрузить прошивку**:
   - `platformio run --target clean`
   - `platformio run --target upload`
   - Дать время загрузиться

---

## 📞 ИНФОРМАЦИЯ ДЛЯ ОТЛАДКИ

Если нужно отправить логи для помощи, приложите:
1. Скриншот браузера (F12 Console)
2. Вывод последних 50 строк Serial Monitor
3. Результат `curl http://192.168.4.1/status`
4. Что именно видите вместо результата

---

**Версия**: 1.0.1 (Fixed Scanning)  
**Дата**: February 19, 2026  
**Язык**: Ukrainian+English
