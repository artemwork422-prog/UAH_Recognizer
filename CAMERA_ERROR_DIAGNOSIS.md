# 🔧 Диагностика помилки: Camera Probe Failed (0x105)

## Помилка
```
E (1296) camera: Camera probe failed with error 0x105(ESP_ERR_NOT_FOUND)
[ERROR] Camera initialization failed!
```

## 🎯 Причини та рішення

### 1. GPIO Pin Configuration ⚠️ **НАЙПОШИРЕНІША**

#### Проблема
Неправильні номери пінів GPIO для вашої з ESP32 модулю.

#### Рішення - Перевірте пін-ауту вашої плати:

**Для ESP32-CAM (WROVER):**
```
XCLK    = GPIO 0
SIOD    = GPIO 26 (I2C SDA)
SIOC    = GPIO 27 (I2C SCL)
VSYNC   = GPIO 25
HREF    = GPIO 23
PCLK    = GPIO 22
PWDN    = GPIO 32
Y2-Y9   = GPIO 5,18,19,21,36,39,34,35
```

**Для інших платформ:**
```
Nano BLE 33 Sense:     Різні GPIO
Nicla Vision:          Різні GPIO  
Portenta H7:           Різні GPIO
RP2040:                Різні GPIO
```

#### Як виправити:
1. Знайти документацію вашої плати
2. Перевірити фактичні GPIO пини камери
3. Оновити в `src/CameraHandler.h`:
```cpp
#define XCLK_GPIO_NUM 0    // ← Ваш номер
#define SIOD_GPIO_NUM 26   // ← Ваш номер
// ... та все решта
```

---

### 2. Кабель або Контакти ❌

#### Проблема
- Камера не підключена
- Міцно зламаний кабель
- Окислені контакти
- Неправильна полярність

#### Перевірка
```
1. Натиснути кнопку "Stop" у серійному моніторі
2. Перевірити весь Wi-Fi кабель від плати до камери
3. Переpiдключити кабель
4. Перезавантажити файл прошивки
```

---

### 3. Дефектна Камера 📷

#### Проблема
Камера модуль пошкоджена або неправильно ініціалюється.

#### Перевірка
```
Видалити наступні рядки в CameraHandler.h:
    config.fb_location = CAMERA_FB_IN_PSRAM;
    config.jpeg_quality = 10;
    
Замінити на більш базову конфігурацію:
    config.fb_location = CAMERA_FB_IN_DRAM;
    config.jpeg_quality = 15;
```

---

### 4. Проблема з PSRAM 🛑

#### Проблема
ESP32 не має PSRAM або він пошкоджений.

#### Рішення
Edit `platformio.ini`:
```ini
[env:esp32cam]
build_flags = 
    -DBOARD_HAS_PSRAM    ; ← Видалити цей рядок
```

---

### 5. Неправильна конфігурація I2C (SCCB) 📡

#### Проблема
I2C пини (SDA/SCL) конфліктують або неправильні.

#### Перевірка
Вручну встановити I2C на правильні пини в конфіг:
```cpp
config.pin_sccb_sda = 26;  // Мейнстрим формат
config.pin_sccb_scl = 27;
```

---

## 🧪 Діагностичні Кроки

### Крок 1: Перевірити Версію Прошивки

Відкрити `platformio.ini` і переконатися що:
```ini
[env:esp32cam]
platform = espressif32
board = esp32cam
framework = arduino
```

### Крок 2: Очистити Пам'ять та Перебудувати

```bash
# У терміналі PlatformIO:
pio run --target clean
pio run --target upload
```

### Крок 3: Перевірити Серійний Вихід

Під час завдання повинне бути:
```
[SETUP] Initializing camera...
[CAMERA] Initializing camera with GPIO configuration...
[CAMERA] Sensor model: PID=0x2642 VER=0x1a
```

Якщо замість цього:
```
E (1296) camera: Camera probe failed
[CAMERA] ERROR: Initialization failed with code 0x105
```

Тоді проблема з GPIO або камерою.

---

## 🔌 Контрольний Список для ESP32-CAM

- [ ] Камера фізично підключена до модулю
- [ ] Кабель не пошкоджений
- [ ] GPIO пини збігаються з платою
- [ ] PSRAM увімкнений (якщо є)
- [ ] Напруга подається поточно (3.3V для ESP32)
- [ ] Перевірено I2C (SCCB) контакти

---

## 📋 Останнього Замісток Трапу

Якщо все вищеперелічене не допомогло:

1. **Спробуйте приклад Arduino IDE:**
   - File → Examples → ESP32 → Camera
   - Оберіть наприклад "Camera Web Server"
   - Апдейтьте GPIO пини
   - Завантажте і перевірьте

2. **Перевірити Іншу Прошивку:**
   - Спробуйте інший камера-код з GitHub
   - Це покаже чи проблема в коді або в аппаратному забезпеченні

3. **Замініть Камеру:**
   - Якщо все інше не спрацює, камера найімовірніше дефектна

---

## ✅ Після Виправлення

Коли камера успішно ініціалізується, ви маєте бачити:

```
[SETUP] Initializing camera...
[CAMERA] Initializing camera with GPIO configuration...
[CAMERA] XCLK: GPIO0, PCLK: GPIO22
[CAMERA] VSYNC: GPIO25, HREF: GPIO23
[CAMERA] SCCB SDA: GPIO26, SCCB SCL: GPIO27
[CAMERA] Data pins: Y2-Y9 on GPIO 5,18,19,21,36,39,34,35
[CAMERA] Sensor model: PID=0x2642 VER=0x1a
[CAMERA] Sensor initialized successfully!
[OK] ✓ Camera initialized
[READY] ✓ System ready for scanning!
```

---

**Якщо помилка залишається, лашкаво надайте:**
- Точний номер модулю ESP32 (WROOM/WROVER)
- Версію бібліотеки Arduino
- Вихід серійного монітора повністю
- Фото відключення кабелю

