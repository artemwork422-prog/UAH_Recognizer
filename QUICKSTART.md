# ⚡ ШВИДКИЙ СТАРТ - 5 ХВИЛИН

## 1️⃣ Установка (30 сек)

```bash
# Якщо ще не встановлено PlatformIO:
# Скачати VS Code: https://code.visualstudio.com
# Встановити "PlatformIO IDE" розширення
```

## 2️⃣ Підготовка Моделі (3 хв)

### Варіант A: Готова модель edge-impulse
```bash
# 1. Завантажити готову модель звідси:
#    https://studio.edgeimpulse.com/...your-project

# 2. Натиснути "Deployment" → "Arduino Library"

# 3. Скачати ZIP файл

# 4. Розпакувати в: lib/Robotics_Practice_inferencing/
```

### Варіант B: Тренування власної моделі (15+ хв)
```bash
# 1. Реєстрація: https://studio.edgeimpulse.com
# 2. Завантажити пак фотографій:
#    - 50 фото кожного номіналу (10, 20, 50, 100, 200, 500)
#    - Розміри ~320x240, JPEG
# 3. Create impulse с Classification або Object Detection
# 4. Натренувати
# 5. Export → Arduino Library
```

## 3️⃣ Компіляція (1 хв)

```bash
# 1. Відкрити VS Code в папці проекту

# 2. Натиснути відповідь в VS Code або в терміналі:
pio run -e esp32cam -t upload --upload-port COM3

# 3. Чекати завантаження (~60 сек)
```

## 4️⃣ Тестування (1 хв)

```
1. Відкрити монітор (платформієнте внизу > open serial monitor)
   Або вручну:
   pio device monitor -b 115200

2. Побачити: "Wi-Fi Started. IP: 192.168.4.1"

3. На телефон: підключитись до WiFi "UAH_Scanner" (пароль: 12345678)

4. Відкрити браузер: http://192.168.4.1

5. Натиснути "SCAN" з купюрою перед камерою

6. Результат: "5 UAH (87%)" або аналогічно
```

## 📱 Веб-Інтерфейс

```
┌──────────────────────┐
│  ВІДЕО ПОТІК LIVE    │
├──────────────────────┤
│                      │
│    [ВІДЕО]           │
│                      │
├──────────────────────┤
│  5 UAH (87%) ⭐      │
├──────────────────────┤
│  [   SCAN BUTTON   ] │
├──────────────────────┤
│ FPS: 30    Status: ✓ │
└──────────────────────┘
```

## 🐛 Швидкі Рішення Проблем

### Проблема: "Camera Init Failed!"
```
✓ Перевірте GPIO піни у CameraHandler.h
✓ Переконайтесь що ESP32-CAM правильно підключена
✓ Отримайте сброс кнопкою "EN" на платі
```

### Проблема: "memory Error"
```
✓ Зменшіть якість JPEG у CameraHandler.h:
  config.jpeg_quality = 5;
✓ Перевірте доступну PSRAM у монітор
```

### Проблема: WiFi не видна
```
✓ Перевірте GPIO налаштування
✓ Подивіться монітор - повинна бути IP адреса
✓ Розташування ESP32-CAM - деякі версії мають GPIO шум
```

### Проблема: "AI Error" при сканування
```
✓ Переконайтесь модель завантажена у lib/
✓ Перевірте model_metadata.h існує
✓ Перевірте розмір входу моделі:
  EI_CLASSIFIER_DSP_INPUT_FRAME_SIZE мав бути 320*240
```

### Проблема: Занадто повільно
```
✓ Зменшіть JPEG якість (краще стиснення)
✓ Зменшіть роздільну здатність на QVGA
✓ Створіть окремий task для inference (advanced)
```

## 📊 Контроль Пам'яті

В монітор серійного порту отримаєте:

```
✓ Camera initialized
PSRAM Available: YES ✓
Free PSRAM: 2097152 bytes  ← СТАРТ
Free Heap: 180000 bytes

>>> При сканування:
🔍 Scanning...
✓ RGB buffer allocated in PSRAM
Free PSRAM: 1867776 bytes  ← ЗАНЯТО RGB

✓ Inference done: 1250ms
✓ RGB buffer freed
Free PSRAM: 2097152 bytes  ← ПОВЕРНУЛОСЬ!
```

Якщо PSRAM не повертається - значить витік ☠️

## ⚙️ Налаштування для Оптимізації

### Якщо пам'ять критична:

```cpp
// У CameraHandler.h:
config.jpeg_quality = 5;        // Нижче = менше пам'яті
config.frame_size = FRAMESIZE_QQVGA;  // 160x120 (для моделей меньш розмірів)
config.fb_count = 1;            // Одна буфер замість двох (менш плавне)
```

### Якщо повільно:

```cpp
// У CameraHandler.h:
config.xclk_freq_hz = 20000000; // Вже максимум
config.jpeg_quality = 8;        // Краще якість = більш інформативно для AI

// У main.cpp:
// Зменшити затримку:
delay(50);  // Замість del(200)
```

## 🔧 Налаштування GPIO (якщо потрібні інші піни)

В main.cpp:
```cpp
#define TRIGGER_PIN 13  // Кнопка (змініть на потрібний GPIO)
#define BUZZER_PIN  12  // Зумер (змініть на потрібний GPIO)
```

В CameraHandler.h - змініть GPIO константи на початку файлу.

## 📱 Мобільна Оптимізація

Веб-інтерфейс автоматично адаптується під розміри екрану.

Для найкращого результату:
- Використовуйте горизонтальну орієнтацію
- Яскраве світло на купюру
- Камера на відстані 10-20 см
- Купюра повинна бути цілком у зоні видимості

## 🎯 Чеклист Перед Запуском

- [ ] Модель завантажена в lib/Robotics_Practice_inferencing/
- [ ] model_metadata.h існує
- [ ] GPIO піни правильні
- [ ] PSRAM показує "Available: YES"
- [ ] Завантажено на ESP32-CAM
- [ ] Монітор показує "Wi-Fi Started"
- [ ] WiFi "UAH_Scanner" видна на мобільному
- [ ] Браузер відкриває http://192.168.4.1
- [ ] Відео потік працює
- [ ] SCAN повертає результат

## 🆘 Технічна Підтримка

Вивести максимум інформації:

```bash
# Увімкнути verbose компіляцію:
pio run -e esp32cam -t upload -v

# Очистити та перебудувати:
pio run -e esp32cam -t clean
pio run -e esp32cam -t upload
```

Перевірити лог в монітор:
```
Serial.println("DEBUG: точка 1");
Serial.printf("Value: %d\n", some_var);
```

---

**✅ Все готово! Натисніть "SCAN" та розпізнайте купюру! 💵**
